export enum AuthEvents {
  AUTH_WILDCARD = 'auth.*',
  AUTH_TOKEN_WILDCARD = 'auth.token.*',
  AUTH_TOKEN_CREATED = 'auth.token.created',
  AUTH_TOKEN_UPDATED = 'auth.token.updated',
  AUTH_TOKEN_DELETED = 'auth.token.deleted',
}
